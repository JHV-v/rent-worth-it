# Redis 连接 URL（访问计数存储）
# 不配置时，访问计数将自动降级到 localStorage（不影响功能，只是无法跨设备聚合）
#
# 同机部署（推荐）：
# REDIS_URL=redis://localhost:6379
#
# 带密码：
# REDIS_URL=redis://:your_password@localhost:6379
#
# 远程 Redis：
# REDIS_URL=redis://:password@redis.example.com:6379
REDIS_URL=

# 站点公开 Origin，用于 /api/visit-count 的 Origin 校验
# 必须与浏览器实际访问的地址（协议 + 域名 + 端口）一致；未配置时只允许本机来源
# 示例：
# NEXT_PUBLIC_SITE_ORIGIN=https://jhx1ng.me
# NEXT_PUBLIC_SITE_ORIGIN=http://117.72.219.13:3000
NEXT_PUBLIC_SITE_ORIGIN=
